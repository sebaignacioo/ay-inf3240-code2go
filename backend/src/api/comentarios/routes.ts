import express from 'express';

import middlewares from '../middlewares';
import comentariosControllers from './controllers';

const comentariosRoutes = express.Router();

export default comentariosRoutes;
